import sys
input = sys.stdin.readline
answer = []
for _ in range(7):
    n = input().strip()
    if int(n)%2 == 0:
        pass
    else:
        answer.append(int(n))
if len(answer) > 0:
    print(sum(answer))
    print(min(answer))
else:
    print(-1)
